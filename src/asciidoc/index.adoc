= Spring Auth API Documentation
:toc: right
:toclevels: 4
:toc-title: Table of Contents
ifndef::snippets[]
:snippets: ../../target/generated-snippets
endif::[]

The HTTP examples below are generated by Spring REST Docs during integration tests.
If an endpoint or payload changes, the related test fails and this document cannot be rebuilt from stale examples.

== 1 Auth Endpoints

=== 1.1 Login
This is an example output for the `POST /auth/login` endpoint.

.request
include::{snippets}/auth/login/http-request.adoc[]

.response
include::{snippets}/auth/login/http-response.adoc[]

It gives informations about the authenticated user along with a JWT token.

==== 1.1.1 Error Response - 400 - Bad Request
These are example outputs for the `POST /auth/login` endpoint for bad request.

===== 1.1.1.1 Missing Login
This is an example output when the login field is missing in the request.

.request
include::{snippets}/auth/login-missing-login/http-request.adoc[]

.response
include::{snippets}/auth/login-missing-login/http-response.adoc[]

It returns a 400 Bad Request error indicating that the login field is required.

===== 1.1.1.2 Missing Password
This is an example output when the password field is missing in the request.

.request
include::{snippets}/auth/login-missing-password/http-request.adoc[]

.response
include::{snippets}/auth/login-missing-password/http-response.adoc[]

It returns a 400 Bad Request error indicating that the password field is required.

===== 1.1.1.3 Invalid Email Format
This is an example output when the email format is invalid in the request.

.request
include::{snippets}/auth/login-invalid-email-format/http-request.adoc[]

.response
include::{snippets}/auth/login-invalid-email-format/http-response.adoc[]

It returns a 400 Bad Request error indicating that the email format is invalid.

===== 1.1.1.4 Empty Body
This is an example output when the request body is empty.

.request
include::{snippets}/auth/login-empty-body/http-request.adoc[]

.response
include::{snippets}/auth/login-empty-body/http-response.adoc[]

It returns a 400 Bad Request error indicating that the request body is missing.

===== 1.1.1.5 Malformed JSON
This is an example output when the request body contains malformed JSON.

.request
include::{snippets}/auth/login-malformed-json/http-request.adoc[]

.response
include::{snippets}/auth/login-malformed-json/http-response.adoc[]

It returns a 400 Bad Request error indicating that the request body is malformed.

===== 1.1.1.6 SQL Injection Attempt Login
This is an example output when the login field contains a SQL injection attempt.

.request
include::{snippets}/auth/login-sql-injection-attempt-login/http-request.adoc[]

.response
include::{snippets}/auth/login-sql-injection-attempt-login/http-response.adoc[]

It returns a 400 Bad Request error indicating that the email format is invalid.

===== 1.1.1.7 SQL Injection Attempt Password
This is an example output when the password field contains a SQL injection attempt.

.request
include::{snippets}/auth/login-sql-injection-attempt-password/http-request.adoc[]

.response
include::{snippets}/auth/login-sql-injection-attempt-password/http-response.adoc[]

It returns a 400 Bad Request error.

==== 1.1.2 Error Response - 415 - Unsupported Media Type
These are example outputs for the `POST /auth/login` endpoint for unsupported media type.

===== 1.1.2.1 Wrong Media Type
This is an example output when the request Content-Type is incorrect.

.request
include::{snippets}/auth/login-wrong-media-type/http-request.adoc[]

.response
include::{snippets}/auth/login-wrong-media-type/http-response.adoc[]

It returns a 415 Unsupported Media Type error indicating that the Content-Type is not supported.

==== 1.1.3 Error Response - 401 - Unauthorized
These are example outputs for the `POST /auth/login` endpoint for unauthorized access.

===== 1.1.3.1 Wrong Password
This is an example output when the password provided is incorrect.

.request
include::{snippets}/auth/login-wrong-password/http-request.adoc[]

.response
include::{snippets}/auth/login-wrong-password/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the password is invalid.

===== 1.1.3.2 Non-Existent User
This is an example output when the user does not exist.

.request
include::{snippets}/auth/login-non-existent-user/http-request.adoc[]

.response
include::{snippets}/auth/login-non-existent-user/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the credentials are invalid.

=== 1.2 Register
This is an example output for the `POST /auth/register` endpoint.

.request
include::{snippets}/auth/register/http-request.adoc[]

.response
include::{snippets}/auth/register/http-response.adoc[]

It registers a new user and gives informations about the created user along with a JWT token.

==== 1.2.1 Error Response - 400 - Bad Request
These are example outputs for the `POST /auth/register` endpoint for bad request.

===== 1.2.1.1 Missing First Name
This is an example output when the first name field is missing in the request.

.request
include::{snippets}/auth/register-missing-first-name/http-request.adoc[]

.response
include::{snippets}/auth/register-missing-first-name/http-response.adoc[]

It returns a 400 Bad Request error indicating that the first name field is required.

===== 1.2.1.2 Missing Last Name
This is an example output when the last name field is missing in the request.

.request
include::{snippets}/auth/register-missing-last-name/http-request.adoc[]

.response
include::{snippets}/auth/register-missing-last-name/http-response.adoc[]

It returns a 400 Bad Request error indicating that the last name field is required.

===== 1.2.1.3 Missing Login
This is an example output when the login field is missing in the request.

.request
include::{snippets}/auth/register-missing-login/http-request.adoc[]

.response
include::{snippets}/auth/register-missing-login/http-response.adoc[]

It returns a 400 Bad Request error indicating that the login field is required.

===== 1.2.1.4 Missing Password
This is an example output when the password field is missing in the request.

.request
include::{snippets}/auth/register-missing-password/http-request.adoc[]

.response
include::{snippets}/auth/register-missing-password/http-response.adoc[]

It returns a 400 Bad Request error indicating that the password field is required.

===== 1.2.1.5 Invalid Email Format
This is an example output when the email format is invalid in the request.

.request
include::{snippets}/auth/register-invalid-email-format/http-request.adoc[]

.response
include::{snippets}/auth/register-invalid-email-format/http-response.adoc[]

It returns a 400 Bad Request error indicating that the email format is invalid.

===== 1.2.1.6 Empty Body
This is an example output when the request body is empty.

.request
include::{snippets}/auth/register-empty-body/http-request.adoc[]

.response
include::{snippets}/auth/register-empty-body/http-response.adoc[]

It returns a 400 Bad Request error indicating that the request body is missing.

===== 1.2.1.7 Malformed JSON
This is an example output when the request body contains malformed JSON.

.request
include::{snippets}/auth/register-malformed-json/http-request.adoc[]

.response
include::{snippets}/auth/register-malformed-json/http-response.adoc[]

It returns a 400 Bad Request error indicating that the request body is malformed.

===== 1.2.1.8 SQL Injection Attempt First Name
This is an example output when the first name field contains a SQL injection attempt.

.request
include::{snippets}/auth/register-sql-injection-attempt-first-name/http-request.adoc[]

.response
include::{snippets}/auth/register-sql-injection-attempt-first-name/http-response.adoc[]

It returns a 400 Bad Request error indicating that the first name field is invalid.

===== 1.2.1.9 SQL Injection Attempt Last Name
This is an example output when the last name field contains a SQL injection attempt.

.request
include::{snippets}/auth/register-sql-injection-attempt-last-name/http-request.adoc[]

.response
include::{snippets}/auth/register-sql-injection-attempt-last-name/http-response.adoc[]

It returns a 400 Bad Request error indicating that the last name field is invalid.

===== 1.2.1.10 SQL Injection Attempt Login
This is an example output when the login field contains a SQL injection attempt.

.request
include::{snippets}/auth/register-sql-injection-attempt-login/http-request.adoc[]

.response
include::{snippets}/auth/register-sql-injection-attempt-login/http-response.adoc[]

It returns a 400 Bad Request error indicating that the email format is invalid.

==== 1.2.2 Error Response - 415 - Unsupported Media Type
These are example outputs for the `POST /auth/register` endpoint for unsupported media type.

===== 1.2.2.1 Wrong Media Type
This is an example output when the request Content-Type is incorrect.

.request
include::{snippets}/auth/register-wrong-media-type/http-request.adoc[]

.response
include::{snippets}/auth/register-wrong-media-type/http-response.adoc[]

It returns a 415 Unsupported Media Type error indicating that the Content-Type is not supported.

==== 1.2.3 Error Response - 409 - Conflict
These are example outputs for the `POST /auth/register` endpoint for conflict errors.

===== 1.2.3.1 Duplicate Login
This is an example output when the login provided already exists.

.request
include::{snippets}/auth/register-duplicate-login/http-request.adoc[]

.response
include::{snippets}/auth/register-duplicate-login/http-response.adoc[]

It returns a 409 Conflict error indicating that the login already exists.

==== 1.2.4 Error Response - 403 - Forbidden
===== 1.2.4.1 Missing permission
This is an example output when the caller is not allowed to register a user.

.request
include::{snippets}/auth/register-noPermissions/http-request.adoc[]

.response
include::{snippets}/auth/register-noPermissions/http-response.adoc[]

It returns a 403 Forbidden error.

=== 1.3 Refresh
This is an example output for the `POST /auth/refresh` endpoint.

.request
include::{snippets}/auth/refresh/http-request.adoc[]

.response
include::{snippets}/auth/refresh/http-response.adoc[]

It refreshes the JWT token for the authenticated user.

==== 1.3.1 Error Response - 401 - Unauthorized
These are example outputs for the `POST /auth/refresh` endpoint for unauthorized access.

===== 1.3.1.1 Missing Token
This is an example output when the token is missing in the request.

.request
include::{snippets}/auth/refresh-missing-token/http-request.adoc[]

.response
include::{snippets}/auth/refresh-missing-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 1.3.1.2 Missing Authorization Header
This is an example output when the Authorization header is missing in the request.

.request
include::{snippets}/auth/refresh-missing-authorization/http-request.adoc[]

.response
include::{snippets}/auth/refresh-missing-authorization/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 1.3.1.3 Invalid Token
This is an example output when the token provided is invalid.

.request
include::{snippets}/auth/refresh-invalid-token/http-request.adoc[]

.response
include::{snippets}/auth/refresh-invalid-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token is invalid.

==== 1.3.2 Error Response - 400 - Bad Request
These are example outputs for the `POST /auth/refresh` endpoint for bad request.

===== 1.3.2.1 Empty Body
This is an example output when the request body is unexpectedly provided.

.request
include::{snippets}/auth/refresh-empty-body/http-request.adoc[]

.response
include::{snippets}/auth/refresh-empty-body/http-response.adoc[]

It returns a 400 Bad Request error.

=== 1.4 Update Password
This is an example output for the `PUT /auth/update-password` endpoint.

.request
include::{snippets}/auth/update-password/http-request.adoc[]

.response
include::{snippets}/auth/update-password/http-response.adoc[]

Updates the password for the authenticated user.

==== 1.4.1 Error Response - 401 - Unauthorized
These are example outputs for the `PUT /auth/update-password` endpoint for unauthorized access.

===== 1.4.1.1 Missing Token
This is an example output when the token is missing in the request.

.request
include::{snippets}/auth/update-password-missing-token/http-request.adoc[]

.response
include::{snippets}/auth/update-password-missing-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 1.4.1.2 Missing Body
This is an example output when the request body is missing.

.request
include::{snippets}/auth/update-password-missing-body/http-request.adoc[]

.response
include::{snippets}/auth/update-password-missing-body/http-response.adoc[]

It returns a 400 Bad Request error.

=== 1.5 Logout
This is an example output for the `POST /auth/logout` endpoint.

.request
include::{snippets}/auth/logout/http-request.adoc[]

.response
include::{snippets}/auth/logout/http-response.adoc[]

Invalidates refresh tokens for the authenticated user.

==== 1.5.1 Error Response - 401 - Unauthorized
These are example outputs for the `POST /auth/logout` endpoint for unauthorized access.

===== 1.5.1.1 Missing Token
This is an example output when the token is missing in the request.

.request
include::{snippets}/auth/logout-missing-token/http-request.adoc[]

.response
include::{snippets}/auth/logout-missing-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 1.5.1.2 Malformed Token
This is an example output when the token provided is malformed.

.request
include::{snippets}/auth/logout-malformed-token/http-request.adoc[]

.response
include::{snippets}/auth/logout-malformed-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token is invalid.

=== 1.6 OAuth2 token
This is an example output for the `POST /oauth2/token` endpoint.

.request
include::{snippets}/auth/get-token-with-authcode/http-request.adoc[]

.response
include::{snippets}/auth/get-token-with-authcode/http-response.adoc[]

Exchanges a one-time authentication code for a JWT access token.

==== 1.6.1 Error Response - 404 - Not Found

===== 1.6.1.1 Wrong login
.request
include::{snippets}/auth/get-token-with-auth-code-wrong-login/http-request.adoc[]

.response
include::{snippets}/auth/get-token-with-auth-code-wrong-login/http-response.adoc[]

===== 1.6.1.2 Wrong code
.request
include::{snippets}/auth/get-token-with-auth-code-wrong-code/http-request.adoc[]

.response
include::{snippets}/auth/get-token-with-auth-code-wrong-code/http-response.adoc[]

== 2 User Endpoints

=== 2.1 Get Authenticated User
This is an example output for the `GET /users/me` endpoint.

.request
include::{snippets}/users/me/http-request.adoc[]

.response
include::{snippets}/users/me/http-response.adoc[]

It gives informations about the authenticated user.

==== 2.1.1 Error Response - 401 - Unauthorized
These are example outputs for the `GET /users/me` endpoint for unauthorized access.

===== 2.1.1.1 Missing Authorization Header
This is an example output when the Authorization header is missing in the request.

.request
include::{snippets}/users/me-missing-authorization/http-request.adoc[]

.response
include::{snippets}/users/me-missing-authorization/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 2.1.1.2 Malformed Token
This is an example output when the token provided is malformed.

.request
include::{snippets}/users/me-malformed-token/http-request.adoc[]

.response
include::{snippets}/users/me-malformed-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token is invalid.

===== 2.1.1.3 Expired Token
This is an example output when the token provided is expired.

.request
include::{snippets}/users/me-expired-token/http-request.adoc[]

.response
include::{snippets}/users/me-expired-token/http-response.adoc[]

=== 2.2 Get All Users
This is an example output for the `GET /users/` endpoint.

.request
include::{snippets}/users/all/http-request.adoc[]

.response
include::{snippets}/users/all/http-response.adoc[]

It gives informations about all users.

==== 2.2.1 Error Response - 401 - Unauthorized
These are example outputs for the `GET /users/` endpoint for unauthorized access.

===== 2.2.1.1 Missing Authorization Header
This is an example output when the Authorization header is missing in the request.

.request
include::{snippets}/users/all-missing-authorization/http-request.adoc[]

.response
include::{snippets}/users/all-missing-authorization/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 2.2.1.2 Malformed Token
This is an example output when the token provided is malformed.

.request
include::{snippets}/users/all-malformed-token/http-request.adoc[]

.response
include::{snippets}/users/all-malformed-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token is invalid.

===== 2.2.1.3 Expired Token
This is an example output when the token provided is expired.

.request
include::{snippets}/users/all-expired-token/http-request.adoc[]

.response
include::{snippets}/users/all-expired-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token has expired.

=== 2.3 Get All Users (Including Deleted)
This is an example output for the `GET /users/all-with-deleted` endpoint.

.request
include::{snippets}/users/all-with-deleted/http-request.adoc[]

.response
include::{snippets}/users/all-with-deleted/http-response.adoc[]

It gives informations about all users including those that have been soft-deleted.

=== 2.4 Get Deleted Users
This is an example output for the `GET /users/deleted` endpoint.

.request
include::{snippets}/users/deleted/http-request.adoc[]

.response
include::{snippets}/users/deleted/http-response.adoc[]

It gives informations about all soft-deleted users.

=== 2.5 Promote User to Manager
This is an example output for the `PUT /users/{login}/promote-manager` endpoint.

.request
include::{snippets}/users/promote-manager/http-request.adoc[]

.response
include::{snippets}/users/promote-manager/http-response.adoc[]

It promotes a user to the "MANAGER" role.

==== 2.5.1 Error Response - 401 - Unauthorized
These are example outputs for the `PUT /users/{login}/promote-manager` endpoint for unauthorized access.

===== 2.5.1.1 Missing Authorization Header
This is an example output when the Authorization header is missing in the request.

.request
include::{snippets}/users/promote-manager-missing-authorization/http-request.adoc[]

.response
include::{snippets}/users/promote-manager-missing-authorization/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 2.5.1.2 Malformed Token
This is an example output when the token provided is malformed.

.request
include::{snippets}/users/promote-manager-malformed-token/http-request.adoc[]

.response
include::{snippets}/users/promote-manager-malformed-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token is invalid.

==== 2.5.2 Error Response - 403 - Forbidden
These are example outputs for the `PUT /users/{login}/promote-manager` endpoint for forbidden access.

===== 2.5.2.1 Non-Admin User - Promote User to Manager
This is an example output when a non-admin user attempts to promote a user to manager.

.request
include::{snippets}/users/promote-manager-non-admin/http-request.adoc[]

.response
include::{snippets}/users/promote-manager-non-admin/http-response.adoc[]

It returns a 403 Forbidden error indicating that access is denied.

==== 2.5.3 Error Response - 404 - Not Found
These are example outputs for the `PUT /users/{login}/promote-manager` endpoint for not found errors.

===== 2.5.3.1 User Not Found
This is an example output when the user to be promoted is not found.

.request
include::{snippets}/users/promote-manager-user-not-found/http-request.adoc[]

.response
include::{snippets}/users/promote-manager-user-not-found/http-response.adoc[]

It returns a 404 Not Found error indicating that the user was not found.

==== 2.5.4 Error Response - 409 - Conflict
These are example outputs for the `PUT /users/{login}/promote-manager` endpoint for conflict errors.

===== 2.5.4.1 User Already Manager
This is an example output when the user to be promoted is already a manager.

.request
include::{snippets}/users/promote-manager-user-already-manager/http-request.adoc[]

.response
include::{snippets}/users/promote-manager-user-already-manager/http-response.adoc[]

It returns a 409 Conflict error indicating that the user is already a manager.

===== 2.5.4.2 User Already Admin
This is an example output when the user to be promoted is already an admin.

.request
include::{snippets}/users/promote-manager-user-already-admin/http-request.adoc[]

.response
include::{snippets}/users/promote-manager-user-already-admin/http-response.adoc[]

It returns a 409 Conflict error indicating that the user is already an admin.

=== 2.6 Revoke Manager to User
This is an example output for the `PUT /users/{login}/revoke-manager` endpoint.

.request
include::{snippets}/users/revoke-manager/http-request.adoc[]

.response
include::{snippets}/users/revoke-manager/http-response.adoc[]

It revokes a manager to "USER" role.

==== 2.6.1 Error Response - 401 - Unauthorized
These are example outputs for the `PUT /users/{login}/revoke-manager` endpoint for unauthorized access.

===== 2.6.1.1 Missing Authorization Header
This is an example output when the Authorization header is missing in the request.

.request
include::{snippets}/users/revoke-manager-missing-authorization/http-request.adoc[]

.response
include::{snippets}/users/revoke-manager-missing-authorization/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 2.6.1.2 Malformed Token
This is an example output when the token provided is malformed.

.request
include::{snippets}/users/revoke-manager-malformed-token/http-request.adoc[]

.response
include::{snippets}/users/revoke-manager-malformed-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token is invalid.

==== 2.6.2 Error Response - 403 - Forbidden
These are example outputs for the `PUT /users/{login}/revoke-manager` endpoint for forbidden access.

===== 2.6.2.1 Non-Admin User
This is an example output when a non-admin user tries to revoke a manager role.

.request
include::{snippets}/users/revoke-manager-non-admin/http-request.adoc[]

.response
include::{snippets}/users/revoke-manager-non-admin/http-response.adoc[]

It returns a 403 Forbidden error indicating that access is denied.

==== 2.6.3 Error Response - 404 - Not Found
These are example outputs for the `PUT /users/{login}/revoke-manager` endpoint

===== 2.6.3.1 User Not Found
This is an example output when the user to be revoked is not found.

.request
include::{snippets}/users/revoke-manager-user-not-found/http-request.adoc[]

.response
include::{snippets}/users/revoke-manager-user-not-found/http-response.adoc[]

It returns a 404 Not Found error indicating that the user was not found.

=== 2.7 Promote to Admin
This is an example output for the `PUT /users/{login}/promote-admin` endpoint.

.request
include::{snippets}/users/promote-admin/http-request.adoc[]

.response
include::{snippets}/users/promote-admin/http-response.adoc[]

It promotes a user or manager to "ADMIN" role.

==== 2.7.1 Error Response - 401 - Unauthorized
These are example outputs for the `PUT /users/{login}/promote-admin` endpoint for unauthorized access.

===== 2.7.1.1 Missing Authorization Header
This is an example output when the Authorization header is missing in the request.
.request
include::{snippets}/users/promote-admin-missing-authorization/http-request.adoc[]

.response
include::{snippets}/users/promote-admin-missing-authorization/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 2.7.1.2 Malformed Token
.request
include::{snippets}/users/promote-admin-malformed-token/http-request.adoc[]

.response
include::{snippets}/users/promote-admin-malformed-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token is invalid.

==== 2.7.2 Error Response - 403 - Forbidden
These are example outputs for the `PUT /users/{login}/promote-admin` endpoint for forbidden access.

===== 2.7.2.1 Non-Admin User
This is an example output when a non-admin user tries to promote a user to admin.
.request
include::{snippets}/users/promote-admin-non-admin/http-request.adoc[]

.response
include::{snippets}/users/promote-admin-non-admin/http-response.adoc[]

It returns a 403 Forbidden error indicating that access is denied.

==== 2.7.3 Error Response - 404 - Not Found
These are example outputs for the `PUT /users/{login}/promote-admin` endpoint for not found errors.

===== 2.7.3.1 User Not Found
This is an example output when the user to be promoted is not found.
.request
include::{snippets}/users/promote-admin-user-not-found/http-request.adoc[]

.response
include::{snippets}/users/promote-admin-user-not-found/http-response.adoc[]

It returns a 404 Not Found error indicating that the user was not found.

==== 2.7.4 Error Response - 409 - Conflict
These are example outputs for the `PUT /users/{login}/promote-admin` endpoint for conflict errors.

===== 2.7.4.1 User Already Admin
This is an example output when the user to be promoted is already an admin.
.request
include::{snippets}/users/promote-admin-user-already-admin/http-request.adoc[]

.response
include::{snippets}/users/promote-admin-user-already-admin/http-response.adoc[]

It returns a 409 Conflict error indicating that the user is already an admin.

=== 2.8 Revoke Admin to User
This is an example output for the `PUT /users/{login}/revoke-admin` endpoint.

.request
include::{snippets}/users/revoke-admin/http-request.adoc[]

.response
include::{snippets}/users/revoke-admin/http-response.adoc[]

It revokes a admin to "USER" role.

==== 2.8.1 Error Response - 401 - Unauthorized
These are example outputs for the `PUT /users/{login}/revoke-admin` endpoint for unauthorized access.

===== 2.8.1.1 Missing Authorization Header
.request
include::{snippets}/users/revoke-admin-missing-authorization/http-request.adoc[]

.response
include::{snippets}/users/revoke-admin-missing-authorization/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 2.8.1.2 Malformed Token
This is an example output when the token provided is malformed.
.request
include::{snippets}/users/revoke-admin-malformed-token/http-request.adoc[]

.response
include::{snippets}/users/revoke-admin-malformed-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token is invalid.

==== 2.8.2 Error Response - 403 - Forbidden
These are example outputs for the `PUT /users/{login}/revoke-admin` endpoint for forbidden access.

===== 2.8.2.1 Non-Admin User
This is an example output when a non-admin user tries to revoke an admin role.

.request
include::{snippets}/users/revoke-admin-non-admin/http-request.adoc[]

.response
include::{snippets}/users/revoke-admin-non-admin/http-response.adoc[]

It returns a 403 Forbidden error indicating that access is denied.

==== 2.8.3 Error Response - 404 - Not Found
These are example outputs for the `PUT /users/{login}/revoke-admin` endpoint for not found errors.

===== 2.8.3.1 User Not Found
This is an example output when the user to be revoked is not found.

.request
include::{snippets}/users/revoke-admin-user-not-found/http-request.adoc[]

.response
include::{snippets}/users/revoke-admin-user-not-found/http-response.adoc[]

It returns a 404 Not Found error indicating that the user was not found.

=== 2.9 Downgrade Admin to Manager
This is an example output for the `PUT /users/{login}/downgrade-admin` endpoint.

.request
include::{snippets}/users/downgrade-admin/http-request.adoc[]

.response
include::{snippets}/users/downgrade-admin/http-response.adoc[]

It downgrades a admin to "MANAGER" role.

==== 2.9.1 Error Response - 401 - Unauthorized
These are example outputs for the `PUT /users/{login}/downgrade-admin` endpoint for unauthorized access.

===== 2.9.1.1 Missing Authorization Header
This is an example output when the Authorization header is missing in the request.

.request
include::{snippets}/users/downgrade-admin-missing-authorization/http-request.adoc[]

.response
include::{snippets}/users/downgrade-admin-missing-authorization/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 2.9.1.2 Malformed Token
This is an example output when the token provided is malformed.

.request
include::{snippets}/users/downgrade-admin-malformed-token/http-request.adoc[]

.response
include::{snippets}/users/downgrade-admin-malformed-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token is invalid.

=== 2.10 Delete User
This is an example output for the `DELETE /users/{login}` endpoint.

.request
include::{snippets}/users/delete/http-request.adoc[]

.response
include::{snippets}/users/delete/http-response.adoc[]

It deletes a user.

==== 2.10.1 Error Response - 401 - Unauthorized
These are example outputs for the `DELETE /users/{login}` endpoint for unauthorized access.

===== 2.10.1.1 Missing Authorization Header
This is an example output when the Authorization header is missing in the request.

.request
include::{snippets}/users/delete-missing-authorization/http-request.adoc[]

.response
include::{snippets}/users/delete-missing-authorization/http-response.adoc[]

It returns a 401 Unauthorized error indicating that full authentication is required.

===== 2.10.1.2 Malformed Token
This is an example output when the token provided is malformed.

.request
include::{snippets}/users/delete-malformed-token/http-request.adoc[]

.response
include::{snippets}/users/delete-malformed-token/http-response.adoc[]

It returns a 401 Unauthorized error indicating that the token is invalid.

=== 2.11 Delete User (For Restore)
This is an example output for the `DELETE /users/{login}` endpoint - soft delete.

.request
include::{snippets}/users/delete-for-restore/http-request.adoc[]

.response
include::{snippets}/users/delete-for-restore/http-response.adoc[]

It soft-deletes a user, allowing them to be restored later.

=== 2.12 Permanently Delete User
This is an example output for the `DELETE /users/{login}/permanent` endpoint.

.request
include::{snippets}/users/delete-permanent/http-request.adoc[]

.response
include::{snippets}/users/delete-permanent/http-response.adoc[]

It permanently deletes a user from the database.

=== 2.13 Restore Deleted User
This is an example output for the `PUT /users/{login}/restore` endpoint.

.request
include::{snippets}/users/restore/http-request.adoc[]

.response
include::{snippets}/users/restore/http-response.adoc[]

It restores a previously soft-deleted user.

=== 2.14 Get User By Login
This is an example output for the `GET /users/{login}` endpoint.

.request
include::{snippets}/users/get-by-login/http-request.adoc[]

.response
include::{snippets}/users/get-by-login/http-response.adoc[]

Returns a single user identified by login.

==== 2.14.1 Error Response - 404 - Not Found
.request
include::{snippets}/users/get-by-login-not-found/http-request.adoc[]

.response
include::{snippets}/users/get-by-login-not-found/http-response.adoc[]

=== 2.15 Update User
This is an example output for the `PUT /users/{login}` endpoint.

.request
include::{snippets}/users/update/http-request.adoc[]

.response
include::{snippets}/users/update/http-response.adoc[]

Updates first name, last name, login and main role of the given user.

