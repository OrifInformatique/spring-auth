[source,bash]
----
$ curl 'http://localhost:8080/auth/register' -i -X POST \
    -d '{
  "firstName" : "Test",
  "lastName" : "User",
  "login" : "test.newuser@test.com",
  "password" : "testPassword"
}'
----